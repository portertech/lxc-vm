description "Run docker"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn

script
    ulimit -n 65535
    /usr/bin/docker -d -H unix:///var/run/docker.sock -H tcp://0.0.0.0:4242
end script
